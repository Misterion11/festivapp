# festivapp

## Build Setup

```bash
# install dependencies and start main
$ yarn install
$ yarn dev

# install dependencies and start backend 1 (NEW TERMINAL DON'T CLOSE THE OLD ONE)
$ cd backend
$ yarn install
$ yarn start

# install dependencies and start backend login (NEW TERMINAL DON'T CLOSE THE OLD ONE)
$ cd login
$ yarn install
$ yarn start

```
